# Write a module named ModuleAdd defines a “Add” function which receives two numbers and
# returns it’s Sum
# Write a module named ModuleMul defines a “Mul” function which receives two numbers and
# returns it’s multiplication WITHOUT implicit multiplication but only by using ModuleAdd

# Write a main module that call the Mul functions, located in any external library